name: CI Pipeline - Simulador SFN

# Define QUANDO a esteira vai rodar
on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest # Um servidor Linux limpo que o GitHub nos empresta

    steps:
    - name: Baixar o código (Checkout)
      uses: actions/checkout@v4

    - name: Configurar o ambiente (Node.js)
      uses: actions/setup-node@v4
      with:
        node-version: '20' # Mesma versão que você utiliza no ambiente local
        cache: 'npm'

    - name: Instalar dependências
      run: npm install

    - name: Rodar Testes Unitários do Domínio
      run: npm run test

    - name: Validar a Compilação do Projeto (Build)
      run: npm run build
      env:
        # Passamos uma variável falsa apenas para o Prisma não dar erro de ambiente 
        # durante a etapa de verificação do build na nuvem
        DATABASE_URL: "postgresql://dummy:dummy@localhost:5432/dummy"